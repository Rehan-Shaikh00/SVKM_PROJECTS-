[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -q
# A coroutine that is created but never awaited is silent data loss: that exact
# bug stopped every call transcript, escalation and analytics row from being
# written. Turn it into a hard failure.
filterwarnings =
    error::RuntimeWarning
